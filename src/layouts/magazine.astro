---
import Nav from '../components/mainNav.astro';
let data = await Astro.glob('../pages/*.md');
const {title} = Astro.props
---

    <header class="h-screen">
        <Nav title={title} />
        <div class="flex h-full">
            <section class="w-2/3 bg-[url('/assets/image1.jpg')] bg-cover bg-center">
            </section>
            <section class="w-1/3 flex justify-center items-center">
                <div class="bg-slate-100 p-5 border-2 rounded-xl">
                    <p>w-1/2 flex justify-center items-center</p>
                </div>
            </section>
        </div>
    </header>
    <main class="">
        <section class="prose-sm">
            <slot />
        </section>
    </main>
    <footer>
        <p>footer</p>
    </footer>